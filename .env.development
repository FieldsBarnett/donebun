# Default Convex URLs for local dev (web + Tauri iOS).
# `npx convex dev` writes your real dev deployment URLs to `.env.local`, which overrides these.
VITE_CONVEX_URL=https://harmless-lyrebird-344.convex.cloud
VITE_CONVEX_SITE_URL=https://harmless-lyrebird-344.convex.site
