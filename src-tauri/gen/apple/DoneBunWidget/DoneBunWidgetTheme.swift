import SwiftUI
import TauriWidgets

/// DoneBun design tokens — keep in sync with `src/index.css`.
enum DoneBunWidgetTheme {
    static let canvas = Color(hex: "#ffffff")
    static let surfaceSoft = Color(hex: "#f2f2f7")
    static let primary = Color(hex: "#007aff")
    static let muted = Color(hex: "#8e8e93")
    static let yellow = Color(hex: "#ffcc00")
    static let ink = Color(hex: "#000000")
}
